# 问题6-补全日志与单测

目前项目基本稳定，但无法确认项目功能是否稳定，所以预计需要做以下事情

1.  梳理项目已有链路，分层级绘制为`mermaid`流程图，补全到`doc/开发文档`目录中。前后端都要梳理
2.  基于项目链路，补充jsonl运行日志。目标是后续能通过日志 + 问题描述，直接定位到相关故障代码。前端可以通过rpc接口，将运行日志也记录下来，方便调试(可以通过文件名进行区分)
3.  基于项目链路，编写单元测试。以做到只要有知乎cookie(若没有，可以作为首个测试case进行，以满足后续需要验证抓取信息时所需条件)，就能执行，且可以给出测试结果。这个测试套件可以使用`mocha`，必要的话也可以安装其他项目


请阅读文档，确认当前提供的信息是否足够全量开发。如果有需要确认的部分，请用@todo(worker)进行标注，我会用@todo(codex)进行回复