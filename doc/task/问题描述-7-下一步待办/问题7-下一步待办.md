# 下一步待办

1.  导出json功能有问题，没有导出全部功能。同时需要确认，导入时是否为增量导入，如果有同id数据，应以较新的版本为准
2.  输出的`用户回答`单文件版html中，没有注入index.html这一目录信息，用户信息也为空。需要确认下其他类型的信息页是否也有这类问题
3.  希望有一个类似`https://trailsnap.cn/docs/dev/guide.html`的文档站，用于展示软件功能，以及项目文档。看起来可以用`VitePress`实现

## 待办 3：VitePress 文档站

### 审阅结论

现有信息足以确定技术方向，但还不足以启动全量开发。仓库当前已经存在一套独立官网：源码位于 `homepage` 分支，构建结果由 GitHub Actions 发布到 `gh-pages`，并使用 `zhihuhelp.yaozeyuan.online` 作为资源地址。直接从当前分支部署 VitePress 会覆盖或绕开这套官网，因此必须先确定最终站点关系和正式 URL。

现有内容可以直接作为站点正文，不需要复制维护第二份 Markdown：

- 产品首页：复用根 `README.md` 与 `doc/README.md` 的项目定位、快速流程和使用限制。
- 用户指南：复用 `doc/用户文档` 下的快速开始、功能说明、数据浏览与输出结果、常见问题。
- 开发文档：复用 `doc/开发文档` 下的环境命令、架构、业务流程、三端分工、数据与日志、测试与 Fixture、维护说明。
- 项目信息：补充下载、GitHub、许可证、反馈和贡献入口。

首版建议使用当前最新稳定版 VitePress，不采用预发布 alpha 版；直接以 `doc` 为站点内容根目录，新增 `doc/.vitepress` 配置和首页。默认提供中文界面、稳定英文路由（`/guide/*`、`/dev/*`）、本地全文搜索、浅色/深色主题、移动端导航、页内目录、上下页、最后更新时间、GitHub 编辑入口和 Mermaid 流程图渲染。根命令新增 `docs:dev`、`docs:build`、`docs:preview`，不改变 Electron 的 `start/watch/startgui` 语义。

默认视觉沿用现有蓝色界面和书本图标，首页展示支持的知乎内容、HTML/EPUB 输出、任务流程、数据浏览与诊断日志。首版不做博客、多语言、评论、服务端搜索或 CMS。下载按钮默认链接 GitHub Releases，本期不修改安装包发布流程。

### 待确认

1.  @todo(worker) 请确认新文档站与现有官网的关系及正式地址。此选择决定 VitePress `base`、站内链接和 GitHub Actions 部署方式。

    - 方案 A（推荐）：用新的 VitePress 产品首页和文档站整体替换旧官网，继续使用 `https://zhihuhelp.yaozeyuan.online/`；用户指南位于 `/guide/`，开发文档位于 `/dev/`。旧 `homepage` 分支停止承担部署职责，站点与主项目文档以后在同一分支维护。
    - 方案 B：保留旧官网首页，把 VitePress 挂到 `https://zhihuhelp.yaozeyuan.online/docs/`。部署时需要同时构建 `homepage` 分支和当前分支，再合并为一个 Pages artifact，后续仍需维护两套前端工程。
    - 方案 C：保留旧官网不变，把新文档站独立发布到 `https://yaozeyuan.github.io/zhihuhelp/`。该方案需要确认自定义域名与 GitHub 项目页能否同时使用，并会形成两个站点入口。

@todo(codex) 同意方案A。vitePress使用当前最新版2.0 alpha即可。文档除根目录下的readme.md外，均收拢于doc目录中，然后再分成guide和dev两个文件夹即可。项目发布在https://zhihuhelp.yaozeyuan.online，旧homepage目录可忽略，可以在github-action中移除，后续统一使用master分支进行发布

2.  @todo(worker) 请确认公开文档范围。

    - 方案 A（推荐）：只公开 `doc/用户文档`、当前有效的 `doc/开发文档` 和项目元信息；明确排除处理记录 `doc/task`、历史设计 `doc/项目文档`、fixture、日志及本地配置。
    - 方案 B：在方案 A 基础上增加“历史设计”分区，发布 `doc/项目文档` 并显著标注为可能失效的历史资料；仍排除 `doc/task`。
    - 方案 C：公开 `doc` 下全部 Markdown。此方案会把需求讨论、已失效设计和内部处理记录暴露给搜索引擎，不推荐。

@todo(codex) 同意方案A

3.  @todo(worker) 请确认是否允许本期制作并提交无敏感信息的产品截图。

    - 方案 A（推荐）：使用独立临时测试目录和公开 fixture，制作 4 张脱敏截图：任务管理、运行日志与输出历史、数据浏览、生成后的 HTML/EPUB 示例；不得出现真实 Cookie、账号、私人收藏或本机绝对路径。
    - 方案 B：首版不放界面截图，仅使用图标、吉祥物、文字和功能卡片展示，后续再补。
    - 方案 C：由项目维护者另行提供截图，本期先预留图片位置。

@todo(codex) 同意方案A
