# 3.0.0

借助 Codex 对项目进行了整体梳理和重构。

## 新功能

- 感谢 @HaitaoShi 提供的 [MR](https://github.com/YaoZeyuan/zhihuhelp/pull/159)，支持新版知乎专栏与文章链接前缀。
- 每次任务固定生成 HTML、Markdown 和 EPUB 三种格式。Markdown 基于 Pandoc 转换，覆盖目录、信息页、正文页和单文件版，适合全文检索、二次编辑和交给 AI 分析。
- 单文件 HTML 增加完整目录：桌面端提供可折叠侧栏，窄屏设备自动切换为正文顶部目录，并支持页内跳转。
- 数据浏览页支持按回答、文章、想法、问题、用户、收藏夹、专栏和话题查看本地缓存，可从摘要列表进入详情及关联内容。
- 支持将缓存数据导出为可校验的 JSON，并支持增量导入；同 ID 数据按更新时间保留较新版本。新增“一键导出全库”，覆盖缓存实体和关系表。
- 运行日志页增加任务阶段、结构化错误、本会话错误列表、最近五日输出历史和诊断信息导出。
- 任务管理增加全局“跳过抓取”，并支持粘贴链接后自动识别任务；仍可逐项调整抓取状态、排序、分卷和生成模式。
- 创建项目[官网](https://zhihuhelp.yaozeyuan.online/)，提供使用说明、功能介绍、数据与输出说明、常见问题和开发指南。首页通过版本接口展示最新版本号及 Windows、macOS 下载按钮。
- 文档站开发环境支持将 `/api` 请求映射到仓库内的 API 文件，便于本地验证版本信息接口。

## 界面与输出优化

- 重构 Electron GUI，完善登录、任务管理、运行日志、数据浏览和开发调试页面，补充加载、空状态、局部成功及错误反馈。
- 输出目录改为按书名聚合：每本书拥有独立目录，其下统一存放 `html`、`markdown` 和 `epub`，输出历史可直接打开该书目录。
- HTML/EPUB 信息页补全用户、专栏、收藏夹、话题和混合任务的名称、标识及描述；缺少信息时不再显示空白内容块。
- 统一前后端任务配置 schema，修正图片质量中 `raw`/`hd` 的历史标签差异，并兼容旧配置文件。
- 用户任务同时支持知乎稳定用户 ID 和公开 `url_token`，修复使用用户 UUID 抓取成功却无法生成内容的问题；生成标题和作者主页链接使用规范标识。
- 输出文件名增加 Windows 非法字符、保留名称、超长标题和目录越界保护；同名任务重新生成时只清理当前书籍目录。

## 稳定性修复

- 修复 Windows + Node.js 24 下 EPUB 仅有约 1 KB、实际缺少正文的问题。生成时会校验 EPUB 必需结构、正文、OPF 清单和目录，并确保 `mimetype` 为首个未压缩条目。
- 为 Windows 输出目录清理增加重试，降低杀毒软件、索引服务或文件系统延迟导致的偶发 `ENOTEMPTY`/`EBUSY` 失败。
- 修复问题、混合类型、收藏夹等任务无法正确生成 EPUB 或关联内容的问题。
- 完善抓取结果校验、分页边界、局部失败传播、作者身份关联和损坏缓存诊断，避免把真实数据库或接口错误误报为“没有数据”。
- Markdown 单页转换失败时保留 `.pandoc-failed.md` 回退文件；只有最终文件无法写入时才将任务标记为部分成功或失败。
- 移除 Ant Design 已弃用的 `List` 用法，消除开发者调试页的兼容性警告。

## 架构与开发维护

- Electron 主进程、CLI 和后端业务代码迁移到原生 ESM；sandbox preload 保留为明确的 CommonJS 边界。
- GUI 与 CLI 统一复用初始化、抓取、持久化和生成 workflow，并通过运行上下文隔离配置、SQLite、缓存、日志和输出路径。
- 建立统一的结构化日志契约，补充 `runId`、阶段、子任务、耗时、错误码、脱敏和按日保留策略。
- 建立 Vitest 单元测试、前端组件测试、离线集成测试、知乎响应 fixture、在线 Electron 冒烟测试和文档站路由校验。
- 补充项目架构、抓取与生成流程、IPC、数据结构、日志、测试、发布和故障排查文档。
- 发布包增加 MIT/GPL 双许可证、第三方依赖声明和 Pandoc 对应源码材料；Windows/macOS 构建流程同步校验许可与运行资源。
- 依赖升级至当前版本，包括 Electron 43、Node.js 24、pnpm 11、TypeScript 7、React 19、Vite 8 和 Ant Design 6。


# 2.5.1

1.  解决混合类型/问题类型无法输出 epub 的问题
2.  添加电子书名长度限制, 最长 100 个字符

# 2.5.0

1.  重写整套代码

# 2.4.1

1.  解决无法请求知乎数据接口问题
2.  默认分卷改为每 10000 个问题一本电子书

# 2.4.0

1.  解决无法请求知乎数据接口问题
2.  调整接口请求逻辑, 禁止并发请求接口, 避免对知乎服务器照成过大压力,
3.  通过对 pic1-4 进行轮询的方法, 解决由于知乎 CDN 不稳定导致无法下载图片的问题
4.  支持根据任务配置自动生成电子书标题, 避免来回取名
5.  同步知乎专栏 url 更改

# 1.5.0

1.  切换到使用 yarn 进行构建

# 1.4.0

1.  增加对封禁用户的支持(该功能仅供个人使用, 不对外开放, 有需要可以自行研究代码解开封锁/知乎私信作者)
2.  优化配置展示样式
3.  支持跳过抓取流程, 加快输出电子书速度

# 1.3.0

1.  更新 electron 至 6.0.0(需删除 node_modules 后重新 install)
2.  增加对 debug 模式的判断, 方便开发
3.  将升级检测接口由 easy-mock 迁移至阿里云
